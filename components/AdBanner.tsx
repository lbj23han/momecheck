'use client';

// AdSense 비활성화 — 토스 미니앱 환경에서는 광고 미표시
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function AdBanner({ className }: { className?: string }) {
  return null;
}
